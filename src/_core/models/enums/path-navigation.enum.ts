import {PATH_NAME as PATH} from "./path-name.enum";

export enum NAVIGATION_PATH {
  HOME=`/${PATH.HOME}`,
  HOME_SELECT_PLAYERS=`/${PATH.HOME}/${PATH.SELECT_PLAYERS}`,
  HOME_INTRO=`/${PATH.HOME}/${PATH.INTRO}`
}
